<h1>Contacts</h1>

<%= link_to "New Contact", new_contact_path %>

<ul>
  <% @contacts.each do |contact| %>
    <li><%= link_to contact.name, contact_path(contact) %> - <%= contact.number %></li>
  <% end %>
</ul>
